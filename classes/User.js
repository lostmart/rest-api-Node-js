class ClassUser {
	constructor(userName, email, password, birthYear) {
		this.userName = userName
		this.email = email
		this.password = password
		this.birthYear = birthYear
	}
}

export default ClassUser
